/** @type {import('next').NextConfig} */
const nextConfig = {
    // assetPrefix: process.env.NODE_ENV === 'production' ? 'https://scrum.ideaswork.cn' : '',
    basePath:  '',
};


export default nextConfig;
